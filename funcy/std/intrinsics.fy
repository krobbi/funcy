/*
* Funcy Intrinsics Library
* A library of wrapper functions for intrinsic expressions.
*/

// Put a character to the standard output and return it.
func putChr(character){
	return $(putChr, character);
}
